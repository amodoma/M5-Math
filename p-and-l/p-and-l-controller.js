angular.module('starter.controllers').controller(
		'PandLController',
		['$scope', '$rootScope', 'PandLService',
				function($scope, $rootScope, PandLService) {
					uLog('In PandL controller');
				}]);